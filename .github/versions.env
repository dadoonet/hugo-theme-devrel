# Single source of truth for CI tool pins (Pages, Netlify, version-bump workflow).
# Dependabot does not update these strings — see .github/workflows/update-tool-versions.yml
# netlify.toml copies HUGO_VERSION / GO_VERSION / NODE_VERSION from here.
HUGO_VERSION=0.165.0
GO_VERSION=1.27.1
NODE_VERSION=24
