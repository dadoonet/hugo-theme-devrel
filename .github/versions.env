# Single source of truth for CI tool pins (Pages workflow + version-bump workflow).
# Dependabot does not update these strings — see .github/workflows/update-tool-versions.yml
HUGO_VERSION=0.165.0
GO_VERSION=1.27.1
NODE_VERSION=24
