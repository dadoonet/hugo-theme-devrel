{
  "name": "hugo-theme-devrel-example",
  "private": true,
  "scripts": {
    "build": "hugo --minify && npx pagefind --site public",
    "index": "npx pagefind --site public",
    "test:search": "bash scripts/assert-pagefind-skips-future.sh public",
    "dev": "hugo server"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
